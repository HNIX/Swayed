<%= render "header" %>

<div class="max-w-[1280px] mx-auto px-6 mt-8">
   <div class="w-full flex flex-col md:flex-row">
      <%= render 'sidebar' %>
          <main class="max-w-xl px-4 sm:px-6 lg:pl-12 lg:pr-0">
            <%= render "form", company: @company %>
          </main>
  </div>
</div>

