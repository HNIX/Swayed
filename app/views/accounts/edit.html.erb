<div class="flex flex-wrap">
  <div class="w-full mb-4 lg:w-1/4 lg:pr-4">
    <%= render partial: "shared/account_navbar" %>
  </div>

  <div class="w-full lg:w-3/4 lg:pl-4">
    <div class="card-panel-settings">
      <div class="mb-4 flex items-center">
        <%= account_avatar @account, class: "flex-shrink-0 rounded-full mr-4 h-8 w-8" %>
        <h1 class="h3 text-black dark:text-white">
          <%= link_to @account.name, @account, class: "text-black dark:text-white" %>
          <%= render_svg "icons/cheveron-right", styles: "fill-current icon-sm text-gray-600 dark:text-gray-300 inline-block" %>
          <%= t(".title") %>
        </h1>
      </div>

      <%= render 'form', account: @account %>
    </div>

    <%= render "accounts/transfers/form", account: @account if @account.can_transfer?(current_user) %>
  </div>
</div>
